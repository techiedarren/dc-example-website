<% if (session && session.timestamp) { %>

<style>
    .admin {
        width:100%;
        height:30px;
        background: red;
        color: white;
        text-align: right;
        padding:5px;
        border-bottom: 1px solid #8B0000;
    }

    .admin a {
        color: white;
    }
</style>

<div class="admin">
    Time Machine: <%- moment(Number(session['timestamp'])).format('dddd, MMMM Do YYYY, h:mm:ss a') %>,
    <a href="/preview/current">&#9746;</a>
</div>

<% } %>